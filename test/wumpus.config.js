/** @type {import('../typings').IConfig} */
const config = {
    "handlers": {
        "commands": true,
        "events": true
    },
    "intents": [
        "Guilds",
        "GuildMessages"
    ]
}

module.exports = config;